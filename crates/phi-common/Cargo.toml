[package]
name = "phi-common"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0"

[dependencies]
# 配置加载（与 config.rs 的依赖一致）
config = "0.15"
once_cell = "1.20"
serde = { version = "1.0", features = ["derive"] }
sha2 = "0.10"
# config.rs 内部使用：调试日志 / merkle/watermark 配置类型
tracing = "0.1"
ed25519-dalek = "2"
hex = "0.4"

[lints]
workspace = true
